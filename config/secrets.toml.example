# Copy to secrets.toml and fill in keys. secrets.toml is gitignored.
#
#   cp config/secrets.toml.example config/secrets.toml
#
# Keys are matched by provider name from settings.toml
# (e.g. [providers.openai] → openai = "…").

[keys]
# openai = "sk-…"
# deepseek = "sk-…"
